name: E2E Tests

# Trigger on pull requests targeting main branch
on:
  pull_request:
    branches: [main]

# Configuration constants - Update these values for easy maintenance
# These values are used throughout the workflow and passed to composite actions
# Note: GitHub Actions doesn't support workflow-level variables, so values are used directly

jobs:
  # Setup job: Install dependencies and cache artifacts for reuse by test jobs
  setup:
    name: Setup and Cache
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup test environment
        uses: ./.github/actions/setup-test-environment
        with:
          node-version: '20'
          java-version: '17'
          java-distribution: 'temurin'

      - name: Setup Playwright browsers
        uses: ./.github/actions/setup-playwright

      - name: Upload dependencies cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-deps-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-deps-

  # Test jobs: Run in parallel using matrix strategy
  # Note: Test matrix configuration is also available in .github/workflows/test-matrix.json
  # for reference and potential future tooling. The matrix must be defined inline here
  # as GitHub Actions doesn't support loading matrix from external files natively.
  test:
    name: Test ${{ matrix.suite }}
    needs: setup
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - suite: Auth + Student
            test_paths: e2e/tests/auth/ e2e/tests/student/
            artifact_name: auth-student
          - suite: Supervisor
            test_paths: e2e/tests/supervisor/
            artifact_name: supervisor
          - suite: Admin
            test_paths: e2e/tests/admin/
            artifact_name: admin

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup test environment
        uses: ./.github/actions/setup-test-environment
        with:
          node-version: '20'
          java-version: '17'
          java-distribution: 'temurin'

      - name: Setup Playwright browsers
        uses: ./.github/actions/setup-playwright

      - name: Verify dependencies
        shell: bash
        run: |
          if [ ! -d "node_modules" ] || [ ! -f "node_modules/.bin/playwright" ]; then
            echo "Dependencies not found, installing..."
            npm ci
          else
            echo "Dependencies found in cache"
          fi

      - name: Set test environment variables
        uses: ./.github/actions/test-env-vars
        with:
          project-id: 'demo-test'
          base-url: 'http://localhost:3000'

      - name: Start Firebase Emulators
        uses: ./.github/actions/firebase-emulators
        with:
          action: start
          project-id: 'demo-test'
          max-attempts: '30'
          emulator-ui-port: '4000'

      - name: Verify Emulators Ready
        shell: bash
        run: |
          echo "Verifying Firebase emulators are ready..."
          # Wait a bit more for emulators to fully initialize
          sleep 3
          # Check Auth emulator (HTTP endpoint)
          if curl -f -s http://localhost:9099 > /dev/null 2>&1; then
            echo "✓ Auth emulator is ready"
          else
            echo "⚠ Auth emulator HTTP check failed (may still be starting)"
          fi
          # Check Emulator UI (already verified by firebase-emulators action, but double-check)
          if curl -f -s http://localhost:4000 > /dev/null 2>&1; then
            echo "✓ Emulator UI is ready"
          else
            echo "✗ Emulator UI not responding"
            exit 1
          fi
          # Firestore uses gRPC, not HTTP, so we can't check it with curl
          # The firebase-emulators action already verified the UI is up, which means emulators are ready
          echo "✓ All emulators are ready (Firestore uses gRPC, verified via UI)"

      - name: Run Tests
        shell: bash
        run: |
          echo "Running tests for: ${{ matrix.suite }}"
          echo "Test paths: ${{ matrix.test_paths }}"
          echo "Environment check:"
          echo "  E2E_TEST=$E2E_TEST"
          echo "  CI=$CI"
          echo "  FIREBASE_AUTH_EMULATOR_HOST=$FIREBASE_AUTH_EMULATOR_HOST"
          echo "  FIRESTORE_EMULATOR_HOST=$FIRESTORE_EMULATOR_HOST"
          echo "  NEXT_PUBLIC_FIREBASE_PROJECT_ID=$NEXT_PUBLIC_FIREBASE_PROJECT_ID"
          npx playwright test ${{ matrix.test_paths }} || {
            echo "Tests failed with exit code $?"
            echo "Checking if Next.js server started..."
            curl -f http://localhost:3000 > /dev/null 2>&1 && echo "Next.js server is running" || echo "Next.js server is NOT running"
            exit 1
          }

      - name: Stop Firebase Emulators
        if: always()
        uses: ./.github/actions/firebase-emulators
        with:
          action: stop

      - name: Upload test artifacts
        uses: ./.github/actions/upload-test-artifacts
        with:
          artifact-name: ${{ matrix.artifact_name }}

  # Aggregate results from all test jobs
  aggregate-results:
    name: Aggregate Test Results
    needs: test
    runs-on: ubuntu-latest
    if: always()
    timeout-minutes: 5

    steps:
      - name: Download all test results
        uses: actions/download-artifact@v4
        with:
          pattern: test-results-*
          merge-multiple: true
          path: all-results/

      - name: Upload combined results
        uses: actions/upload-artifact@v4
        with:
          name: all-test-results
          path: all-results/
          retention-days: 30
          if-no-files-found: ignore
